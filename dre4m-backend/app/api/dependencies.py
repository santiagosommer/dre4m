Conexión a bases de datos (o cualquier otro recurso compartido).
Autenticación y autorización (verificación de tokens, permisos de usuario, etc.).
Validaciones de datos que se usan en múltiples endpoints.
Servicios reutilizables (como envío de correos, generación de tokens, etc.).
Configuración global (variables de entorno y parámetros de configuración).
Parámetros comunes para las rutas.